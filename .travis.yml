cache: bundler
language: ruby
install:
  - 'gem update --system'
  - 'gem --version'
  - 'gem install bundler -v 1.16.1'
  - 'bundle --version'
  - bundle
rvm:
  - 2.5.0
  - ruby-head
os:
  - linux
  - osx
addons:
  apt:
    packages:
      - libavahi-compat-libdnssd-dev
notifications:
  email: false
